@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.Darbuotojas>
@{
	ViewData["title"] = "Darbuotojai";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center p-2">
	<span class="title">@ViewData["title"]</span>
    <span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

<div class="d-flex justify-content-center">
	<table class="entitylist table table-striped table-hover">
		<thead>
			<tr>
                <th>
					@Html.DisplayNameFor(model => model.kodas)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.vardas)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.pavarde)
				</th>
				<th>
					@Html.DisplayNameFor(model => model.filialas)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
                <th>
					@Html.DisplayFor(model => item.kodas)
				</th>
				<td>
					@Html.DisplayFor(model => item.vardas)
				</td>
				<td>
					@Html.DisplayFor(model => item.pavarde)
				</td>
				<th>
					@Html.DisplayFor(model => item.filialas)
				</th>
                <td>
					<a href="@Url.Action("Edit", new { id = item.kodas})" class="btn btn-primary"><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
					<a href="@Url.Action("Delete", new { id = item.kodas })" class="btn btn-danger"><i class="fa-solid fa-trash"></i> Šalinti</a>
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>